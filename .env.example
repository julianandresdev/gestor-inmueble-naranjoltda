# Copia este archivo a `.env` y ajusta los valores:
#   cp .env.example .env

# URL de conexión a PostgreSQL.
# Si usas docker-compose.yml de este repo con la configuración por defecto,
# esta URL funciona tal cual:
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/gestor_inmueble?schema=public"

# Secreto usado por Auth.js para firmar tokens de sesión JWT.
# Genera uno con: openssl rand -base64 32
AUTH_SECRET="reemplaza-por-un-secreto-generado"

# Credenciales del administrador inicial (usado solo por el seed):
#   pnpm seed:admin
ADMIN_NOMBRE="Administrador"
ADMIN_USERNAME="admin"
ADMIN_PASSWORD="cambia-esta-contrasena"

# Entorno de la aplicación: development | production
NODE_ENV="development"
